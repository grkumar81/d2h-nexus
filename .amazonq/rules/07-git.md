# Git Rules

Keep changes focused. Review git diff and changed files before finishing.

Never commit secrets.

Do not execute without explicit approval:
- git reset --hard
- git clean -fd
- git push --force
- destructive branch/history operations
