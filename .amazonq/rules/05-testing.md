# Testing Rules

Every meaningful change must consider tests.

Test finance calculations, validation, tenant resolution/isolation, asset lifecycle, notification events, uploads, APIs and important UI flows.

Never weaken tests to make a build pass.

Prefer regression tests for bug fixes.

Never claim tests passed unless they were actually executed.
