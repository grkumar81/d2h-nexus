# Copy to .env.local and adjust for your environment.
# In development the Vite proxy forwards /api to this URL,
# so this variable is only needed when the proxy is bypassed (e.g. production build).
VITE_API_BASE_URL=http://localhost:8080
